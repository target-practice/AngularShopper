import { Component } from '@angular/core';

@Component({
	selector: 'products', // creates custom HTML element <products>
	template: '<h2>Products List</h2>' // the HTML markup to be rendered
})
export class ProductsComponent {
	
	// constructor(argument) {
	// 	// code...
	// }
}